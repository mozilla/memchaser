language: java

before_install:
 - git submodule update --init

before_script:
 - sh -e /etc/init.d/xvfb start

env:
 - DISPLAY=':99.0'

notifications:
  irc:
    channels:
      - "irc.mozilla.org#automation"
    on_success: always
    on_failure: always
    use_notice: true

